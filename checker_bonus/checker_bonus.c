#include "../include/checker_bonus.h"

int	main(int argc, char **argv)
{
	t_build	data;
	t_stack	*a;
	char	*instruction;

	init_data(argc, argv, &data);
	instruction =
	if (is_sorted(a))
	{
		ft_printf("OK\n");
	}
	else
	{
		ft_printf("KO\n");
	}
}